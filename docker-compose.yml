version: '3.8'

services:
  converter-api:
    build: .
    container_name: excel_to_pipe_converter
    ports:
      - "8096:8080"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
